package retrospector.javafx.view.search;

import com.airhacks.afterburner.views.FXMLView;
import java.util.ResourceBundle;

public class SearchView extends FXMLView {
  public SearchView(ResourceBundle bundle) {
    this.bundle = bundle;
  }
}
