package retrospector.javafx.view.media;

import com.airhacks.afterburner.views.FXMLView;
import java.util.ResourceBundle;

public class MediaView extends FXMLView {
  public MediaView(ResourceBundle bundle) {
    this.bundle = bundle;
  }
}
